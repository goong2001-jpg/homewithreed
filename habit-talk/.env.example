# 푸시 알림을 쓸 때만 필요합니다. 비워두면 앱은 앱 내 알림만으로 정상 동작합니다.
# habit-push/README.md 의 절차대로 Cloudflare Worker를 배포한 뒤 값을 채우세요.

# Worker 배포 시 함께 만든 VAPID 공개키 (base64url)
REACT_APP_VAPID_PUBLIC_KEY=

# 배포된 Worker 주소 (끝에 / 없이)
REACT_APP_PUSH_ENDPOINT=
