{
  "name": "habit-push",
  "version": "0.1.0",
  "private": true,
  "description": "톡톡 습관친구 푸시 알림 스케줄러 (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "keys": "node scripts/generate-keys.mjs",
    "test": "node --experimental-strip-types --test src/*.test.mjs"
  },
  "dependencies": {
    "web-push-browser": "^1.4.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.101.0"
  },
  "type": "module"
}
